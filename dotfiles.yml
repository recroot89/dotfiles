---
- hosts: all

  tasks:

    - file:
        force: yes
        src: "{{ curdir }}/files/{{ item }}"
        dest: ~/.{{ item }}
        state: link
      with_items:
        - aliases
        - my_aliases
        - tmux.conf
        - zshrc
